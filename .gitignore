/pkg/
/tmp/
# The directory itself is tracked (see libexec/.gitkeep) — a fresh clone has to
# have somewhere to stage a binary — but no binary may ever be committed.
/libexec/*
!/libexec/.gitkeep
/Gemfile.lock
/.bundle/
*.gem
